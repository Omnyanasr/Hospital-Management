import 'package:flutter/material.dart';

class SymptomChatbotPage extends StatelessWidget {
  const SymptomChatbotPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Hi"),
    );
  }
}
